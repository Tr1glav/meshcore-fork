// Точка входа прошивки. Вся логика — в общей библиотеке lib/meshcore (app_main.cpp):
// её собирают и этот проект, и соседний репозиторий tdeck, поэтому главный цикл живёт
// в одном экземпляре. Здесь остаются только setup()/loop(): Arduino ищет эти символы
// в проекте, а не в библиотеке.
#include "app_main.h"

void setup() { appSetup(); }
void loop()  { appLoop(); }
