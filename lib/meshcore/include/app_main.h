#pragma once

// Общий запуск и главный цикл (lib/meshcore/src/app_main.cpp). Проект платы вызывает их
// из своих setup()/loop(): Arduino требует эти символы в самом проекте, а не в библиотеке.
void appSetup();
void appLoop();

// Точка расширения для проекта платы: своё железо и свой интерфейс. У T-Deck отсюда
// поднимается клавиатура с трекболом и живёт меню; платам Heltec расширять нечего.
// Обе функции слабые и по умолчанию пустые — проект платы просто определяет свои и тем
// самым перекрывает пустышки, без единого #ifdef в общем коде.
void boardSetup();   // в конце appSetup(), когда радио и экран уже подняты
void boardTick();    // каждый проход appLoop(), ничего не блокируя
